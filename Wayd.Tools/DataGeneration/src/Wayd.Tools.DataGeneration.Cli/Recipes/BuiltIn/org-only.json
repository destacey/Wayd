{
  "$schema": "../recipe.schema.json",
  "version": 1,
  "extends": "default",
  "description": "The organization on its own, with no PPM dataset over it. Replaces the --skip-ppm flag.",
  "ppm": {
    "enabled": false
  }
}
