---
'@wayd/mcp': minor
---

The four forecast tools accept an optional `startedWorkFirst` (default true), which counts active backlog work items ahead of proposed ones, each in rank order, since teams usually finish what they have started. Pass false to order backlogs by rank alone.
